<h1>FS3 Combat</h1>

<p>This screen lets you configure several common FS3 Combat settings.  More fine-tuned configuration is available on the <a href="/admin/config">Advanced Configuration</a> screen.</p>

<form method="POST" action="/admin/fs3_combat/update">


<div class="panel panel-primary">
    <div class="panel-heading">
        Skills
    </div>
    
    <div class="panel-body">
        <% @skills.each do |k, v| %>
        <div class="row">
            <div class="col col-xs-12">
            <label class="col-xs-2"><%= k.titleize %>:</label>
            <input name="skill-<%= k %>" type="text" size="20" value="<%= v %>" />
            </div>
        </div>
        <% end %>
    </div>
</div>

<p>
<a href="/admin"><button type="button" class="btn btn-default">Cancel</button></a>
<button type="submit" class="btn btn-primary">Save</button>
</p>
</form>