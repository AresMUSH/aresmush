<p>Here you can edit your game's configuration files.</p>

<div class="alert alert-warning">This section is for experienced users.  Please read the AresMUSH.com tutorials before attempting to configure your game here.</div>

<h1>Game Config</h1>

<ul>
    <% @game_config.sort.each do |f| %>
    <li><a href="/admin/config/edit?path=<%= f %>"><%= File.basename(f) %></a></li>
    <% end %>
</ul>

<h1>Plugin Config</h1>
    <blockquote>
        Quick Links:
        <% @plugin_config.keys.sort.each do |p| %>
        <a href="#<%= p %>"><%= p %></a> | 
        <% end %>
    </blockquote>

    <% @plugin_config.keys.each do |p| %>
        <hr/>
        <a name="<%= p %>"></a>
        <h3><%= p %></h3>

            <p><a href="/help/<%= p %>/index" target="_blank">Configuration Help</a></p>
            
            <% ["config", "help", "locale", "templates"].each do |section| %>
            <b><%= section %></b>
                <ul>
                <% @plugin_config[p][section].each do |f| %>
                  <li><a href="/admin/config/edit?path=<%= f %>&plugin=<%= p %>"><%= File.basename(f) %></a></li>
                <% end %>
                </ul>
            <% end %>
    <% end %>