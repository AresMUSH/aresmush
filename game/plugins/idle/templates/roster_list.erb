<%= header %>
%xh<%= t('idle.roster_title') %>%xn

%xh<%= t('idle.roster_column_titles') %>%xn
<% paginator.items.each do |r| %>
<%= left( r.name, 22 ) -%>
<%= left( r.demographic('gender'), 10 ) -%>
<%= left( r.group("Position"), 20 ) -%>
<%= left( approved(r), 12 ) -%>
<%= contact(r) %>
<% end %>
<%= line_with_text paginator.page_marker %>
<%= t('idle.star_means_contact_required')%>
<%= footer %>