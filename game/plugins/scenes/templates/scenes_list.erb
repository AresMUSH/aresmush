<%= header %>
%xh<%= t('scenes.scenes_title')%>%xn
<% scenes.each do |scene| %>
<%= divider %>
%xh<%= scene.room.name %>%xn <<%= privacy(scene) %>>  <%= organizer(scene) %>
<%= left( t('scenes.players_title'), 10 ) %> <%= characters(scene) %>
<% if scene.room.scene_set && !scene.private_scene %>

<%= scene.room.scene_set%>
<% end %>
<% end %>
<%= footer %>