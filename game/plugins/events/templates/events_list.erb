%l1
%xh<%= t('events.events_title') %>%xn

<% events.each_with_index do |ev, i| %>
<%= left(i+1, 4) %><%= left( ev.title, 30 ) %> <%= event_starts(ev) %>
<% end %>

<%= t('events.url_title') %> <%= view_url %>
%l1