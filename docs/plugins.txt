~~
plugin
- config*.yml
- help*.yml
- locale*.yml
- *.rb (unless it ends in _spec.rb or _specs.rb)

Traditional is to have lib/spec/locale sub-dirs with config/help at the top.

Well-defined interfaces in interface folder


Closely-coupled systems:

FS3 / Chargen / Sheet

Handles / Friends / API