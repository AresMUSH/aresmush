<%= line_with_text t('swrifts.sheet_title', :name => char.name) -%>

<%= left( t('swrifts.iconicf_title'), 20 ) %> <%= left( iconicf_name, 16 ) %> | <%= left( t('swrifts.bennies_title'), 20 ) %> <%= left( iconicf_name, 16 ) %>
<%= left( t('swrifts.race_title'), 20 ) %> <%= left( iconicf_name, 16 ) %> | <%= left( t('swrifts.conviction_title'), 20 ) %> <%= left( iconicf_name, 16 ) %>
<%= left( t('swrifts.rank_title'), 20 ) %> <%= left( iconicf_name, 16 ) %> |
<%= line_with_text t('swrifts.stats_title') -%>
<% stats.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swrifts.skills_title') -%>
<% skills.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swrifts.hinderances_title') -%>
<% hinderances.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swrifts.edges_title') -%>
<% edges.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swrifts.mpowers_title') -%>
<% mpowers.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swrifts.abilities_title') -%>
<% abilities.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swrifts.complications_title') -%>
<% complications.each do |a| %>
<%= a -%>
<% end %>

%ld