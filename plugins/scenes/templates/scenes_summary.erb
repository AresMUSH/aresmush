<%= header %>
<% if paginator.out_of_bounds? %>
<%= paginator.out_of_bounds_msg %>
<% else %>
%xh<%= t('scenes.scenes_title')%>%xn

<% paginator.page_items.each do |scene| %>
<%= scene.id %><%= unshared(scene) %> - <<%= privacy(scene) %>> <%= scene.title %> (<%= organizer(scene) %> / <%= characters(scene) %>)
<% end %>

<%= paginator.page_footer %>
<% end %>
<%= footer %>