<%= header %>
%xh<%= t('idle.idle_title') %>%xn 

%xh<%= t('idle.idle_subtitle') %>%xn 
<% sorted_queue.each do |entry| %>
<%= highlight_color(entry) %><%= left( name(entry), 20 ) %>%xn <%= left( last_on(entry), 13 ) -%>
<%= left( action(entry), 16 ) -%>
<%= lastwill(entry) %> 
<% if idle_notes(entry) %>
%T<%= idle_notes(entry) %>
<% end %>
<% end %>

* - <%= t('idle.previously_warned') %>
<%= footer %>