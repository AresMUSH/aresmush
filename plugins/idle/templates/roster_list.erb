<%= header %>
%xh<%= t('idle.roster_title') %>%xn

%xh<% fields.each do |f| %><%= show_title(f) %> <% end %><%= t('idle.roster_column_titles') %>%xn
<% paginator.items.each do |r| %>
<% fields.each do |f| %><%= show_field(r, f) -%> <% end %> <%= -%>
<%= center( r.roster_played.yesno, 5 ) %>
   <% if r.roster_notes %><%= r.roster_notes %>%R<% end %>
<% end %>
<%= paginator.page_footer %>
%xh<%= t('idle.roster_url_hint', :url => roster_url) %>%xn
All roster characters require a brief application.
<%= footer %>