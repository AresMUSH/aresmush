<%= line_with_text t('swade.sheet_title', :name => char.name) -%>

<%= left( t('swade.iconicf_title')'*', 20 ) %> <%= left( iconicf_name, 16 ) %> | <%= left( t('swade.bennies_title'), 20 ) %> <%= left( iconicf_name, 16 ) %>
<%= left( t('swade.race_title'), 20 ) %> <%= left( iconicf_name, 16 ) %> | <%= left( t('swade.conviction_title'), 20 ) %> <%= left( iconicf_name, 16 ) %>
<%= left( t('swade.rank_title'), 20 ) %> <%= left( iconicf_name, 16 ) %> |
<%= line_with_text t('swade.stats_title') -%>
<% stats.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swade.skills_title') -%>
<% skills.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swade.hinderances_title') -%>
<% hinderances.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swade.edges_title') -%>
<% edges.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swade.mpowers_title') -%>
<% mpowers.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swade.abilities_title') -%>
<% abilities.each do |a| %>
<%= a -%>
<% end %>

<%= line_with_text t('swade.complications_title') -%>
<% complications.each do |a| %>
<%= a -%>
<% end %>

%lf