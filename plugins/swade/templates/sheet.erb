%lh
<%= t('swade.sheet_title', :name => char.name) %>

<%= line_with_text t('swade.iconicf_title') -%>
<%= left ( summary, 80 ) %>

<%= line_with_text t('swade.attributes_title') -%>
<% attrs.each do |a| -%>
<%= a -%>
<% end %>

%lf