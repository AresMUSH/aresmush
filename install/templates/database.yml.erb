database:
    production:
        sessions:
            default:
                hosts:
                    - <%= @db_host %>:<%= @db_port %>
                database: <%= @db_name %>
                username: <%= @db_user %>
                password: <%= @db_password %>
        options:
            raise_not_found_error: false

    test:
        sessions:
            default:
                hosts:
                    - 
                database: 
                username: 
                password: 
        options:
            raise_not_found_error: false