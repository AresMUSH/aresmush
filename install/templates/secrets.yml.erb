# This file is for plugin config info that you want to keep secret, like passwords.  
# It won't be checked into source control in git so keep a backup copy in a safe place.

secrets:
    database:
        password: "<%= @db_password %>"
    
    events:
        calendar: ""
        api_key: ""
